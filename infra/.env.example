# Values here must match apps/api/.env.example's DATABASE_URL / REDIS_URL
# when running the full stack locally against this compose file.
POSTGRES_USER=pablo
POSTGRES_PASSWORD=pablo
POSTGRES_DB=pablo
